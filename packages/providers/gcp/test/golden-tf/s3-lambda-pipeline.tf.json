{
  "terraform": {
    "required_providers": {
      "google": {
        "source": "hashicorp/google",
        "version": "~> 5.0"
      }
    }
  },
  "provider": {
    "google": {
      "project": "${var.project_id}",
      "region": "${var.gcp_region}"
    }
  },
  "variable": {
    "project_id": {
      "type": "string"
    },
    "gcp_region": {
      "type": "string",
      "default": "us-central1"
    }
  },
  "resource": {
    "google_cloudfunctions2_function": {
      "processfn": {
        "name": "ProcessFn",
        "location": "${var.gcp_region}",
        "build_config": [
          {
            "runtime": "nodejs20",
            "entry_point": "handler",
            "source": [
              {
                "storage_source": [
                  {
                    "bucket": "${var.project_id}-artifacts",
                    "object": "processfn.zip"
                  }
                ]
              }
            ]
          }
        ],
        "service_config": [
          {
            "available_memory": "128Mi",
            "timeout_seconds": 30,
            "environment_variables": {
              "IACMP_CLOUD": "gcp",
              "DEST_BUCKET": "OutputBucket.name"
            }
          }
        ]
      }
    },
    "google_cloud_run_v2_service_iam_member": {
      "processfn_public": {
        "project": "${var.project_id}",
        "location": "${var.gcp_region}",
        "name": "${lower(google_cloudfunctions2_function.processfn.name)}",
        "role": "roles/run.invoker",
        "member": "allUsers"
      }
    },
    "google_storage_bucket": {
      "inputbucket": {
        "name": "${var.project_id}-inputbucket",
        "location": "US",
        "force_destroy": true,
        "versioning": [
          {
            "enabled": true
          }
        ],
        "uniform_bucket_level_access": true
      },
      "outputbucket": {
        "name": "${var.project_id}-outputbucket",
        "location": "US",
        "force_destroy": true,
        "versioning": [
          {
            "enabled": false
          }
        ],
        "uniform_bucket_level_access": true
      }
    }
  },
  "output": {
    "ProcessFnFunctionUrl": {
      "value": "${google_cloudfunctions2_function.processfn.service_config[0].uri}"
    },
    "InputBucketBucketName": {
      "value": "${google_storage_bucket.inputbucket.name}"
    },
    "InputBucketBucketUrl": {
      "value": "${google_storage_bucket.inputbucket.url}"
    },
    "OutputBucketBucketName": {
      "value": "${google_storage_bucket.outputbucket.name}"
    },
    "OutputBucketBucketUrl": {
      "value": "${google_storage_bucket.outputbucket.url}"
    }
  }
}
