export interface CloudDomains {
  storage: string;
  compute: string;
  database: string;
  cache: string;
  network: string;
  security: string;
  messaging: string;
  workflow: string;
  monitoring: string;
  ml: string;
}
